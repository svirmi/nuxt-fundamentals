{
  "name": "nuxt-fundamentals",
  "version": "1.0.0",
  "description": "My try and test Nuxt.js project",
  "author": "MS",
  "private": true,
  "scripts": {
    "dev": "nuxt --hostname 0.0.0.0 --port 8000",
    "build": "nuxt build",
    "start": "nuxt start --hostname 0.0.0.0 --port 8000",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "nuxt": "^2.0.0",
    "@nuxtjs/axios": "^5.3.6",
    "@nuxtjs/dotenv": "^1.4.0"
  },
  "devDependencies": {}
}
